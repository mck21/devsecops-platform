# Loki — dev (Minikube). Single binary, filesystem storage, ephemeral.
# helm install loki grafana/loki -n monitoring -f monitoring/loki/values-dev.yaml
deploymentMode: SingleBinary

loki:
  auth_enabled: false
  commonConfig:
    replication_factor: 1
  storage:
    type: filesystem
  schemaConfig:
    configs:
      - from: "2024-01-01"
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: loki_index_
          period: 24h
  limits_config:
    retention_period: 48h

singleBinary:
  replicas: 1
  persistence:
    enabled: false

# Disable the scalable components on Minikube.
backend:
  replicas: 0
read:
  replicas: 0
write:
  replicas: 0
chunksCache:
  enabled: false
resultsCache:
  enabled: false
