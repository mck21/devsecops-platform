apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: backend
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  host: backend
  subsets:
    - name: blue
      labels:
        version: blue
    - name: green
      labels:
        version: green
