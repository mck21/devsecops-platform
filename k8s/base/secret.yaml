# Template only — values match dev postgres defaults for Minikube.
# Override in staging/production via External Secrets Operator (Phase 6).
# Never commit real production credentials.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
type: Opaque
stringData:
  DATABASE_URL: postgresql://postgres:postgres@postgres:5432/featureflags
